# [CADON-Ver-03] 과적합 검증 통과 및 파일럿 운영 최종 인계 보고서 (Claude 전달용)

> **문서 목적**: 본 문서는 `CADON-Ver-03` 프로젝트의 데이터 정합성 이슈(조치 1~5) 해결, 1차 및 2차 도면의 과적합(Overfitting) 완전 해소 실측 비교, 그리고 파일럿 운영을 위한 골든 데이터셋 동결 및 6대 무결성 SOP를 **클로드(Claude)에게 온전히 인계하기 위해 작성된 최종 공식 문서**입니다.

- **작성 일시**: 2026-09-20 18:26 (KST)
- **과적합 검증 최종 판정**: **통과 (APPROVED & PASSED)**
- **개발/미리보기 서버**: `http://localhost:4005` (Next.js Dev Server)
- **EGDesk API / Local DB**: `http://localhost:8080` (SQLite DB 연동)
- **동결된 골든 데이터셋 (Golden Dataset)**:
  - **1차 기준 검증 케이스**: `case_1789766302590` (`테스트용DWG-2.dwg`, 7.08 MB)
  - **2차 신규 검증 케이스**: `case_1789894718545` (`2503-021_sample_1.dwg`, 2.04 MB)

---

## Ⅰ. 핵심 무결성 조치 내역 (조치 1 ~ 조치 5)

과거 수치 혼선(107 / 123 / 133 / 145 및 중량 불일치)의 기술적 원인을 전수 규명하고 DB 및 소스 코드에 영구 반영하였습니다.

### 1. [조치 1] 조립도 16장 DB 배제 영구 기록 (`is_quote_included = 0`)
- **원인 규명**: 
  - 과거 보고의 "조립도 12건 제외"는 단순 화면 문자열 필터에 불과하여 DB에는 전량 1로 적재되어 있었음.
  - 실제 조립도는 한글 명칭('조립도') 12장 외에 영문 명칭 조립체(`MAIN/SUB CHAIN DRIVE 01~04-000`, `SR1-000`) 4장이 더 존재하여 총 16장이었음.
- **조치 완료**:
  - `drawings` 테이블의 조립도 16장 전수에 대해 `is_quote_included = 0`, `exclude_reason = '조립도 (가공품 제외)'` 물리적 기록 완료.
  - `src/lib/cad-pipeline.ts` 파이프라인 수정: 향후 어떤 도면이 투입되더라도 적재 시점에 조립도가 자동으로 `is_quote_included = 0` 처리되도록 영구 보강.
  - 단품 107장은 `is_quote_included = 1`로 명확히 분리.

### 2. [조치 2] 중량 집계 단일화 및 모수 일치 검산(`isCountMatched`) 신설
- **원인 규명**:
  - 피처 단순 합산과 MRP 엔진 간 중량 왜곡의 원인은 **"동일 도번(dwgNo)을 공유하는 복수 시트 도면"** 때문이었음.
  - 기존 MRP 엔진이 Map Key를 도번(`dwgNo`)으로 매핑하면서 뒤쪽 시트 피처가 앞쪽 시트 피처를 덮어써 왜곡 발생.
- **조치 완료**:
  - `mrp-engine.ts`를 고유 `drawing_id` 기반 1:1 매핑으로 전면 개편.
  - **검산 규칙 신설**: `isCountMatched`(모수 일치) 검증을 추가하여 **건수(모수)가 다르면 합계 중량이 같아도 중량 보존 실패(`isWeightConserved = false`)로 엄격 차단**.
  - 단품 107건 기준 피처 합산치 = MRP 엔진 산출치 = **317.505 kg (오차 0.000 kg)**으로 100% 일치.

### 3. [조치 3 & 조치 5] 단위 표기 분리 및 과거 오보고 폐기/정정
- **과거 133건 유래 규명**: 총 BOM 145건에서 과거 부분 조립도 12건을 단순 차감했던 수치(145 - 12 = 133)였음을 확인하고 완전히 폐기.
- **단위 분리 원칙 확립**:
  - 도면 수: **장** (Sheets)
  - BOM 품목 수: **건** (Items)
  - 원자재 중량: **kg** (Mass)
- **마스터 매칭 정정**: 과거 '단가풀 F7 1건' 오보고를 폐기하고, 실제 후보 157건 / 고유 매칭 68품목으로 정정 반영.

### 4. [조치 4] 파싱 Idempotent(멱등성) 처리 및 중복 누적 데이터 정리
- **원인 규명**: 동일 파일 3회 파싱으로 `cad_objects`가 60,003건 중복 누적되어 있었음.
- **조치 완료**:
  - 구버전 2회분(40,002건) 물리 삭제 완료.
  - 구버전 `cad_parse_runs`는 감사(Audit) 이력 보존을 위해 `status = 'SUPERSEDED'`로 전이.
  - 최신 1회분(`parse_1789895993788`) 20,001건만 유효 유지.
  - `cad-pipeline.ts`에 재파싱 시 기존 객체 자동 정리(Replace) 멱등성 로직 영구 반영.

---

## Ⅱ. [1차 기준 케이스 vs 2차 검증 케이스] 최종 확정 실측 비교표

> **검증 완료 후 공식 동결된 골든 데이터셋 실측치** (단위 분리 엄수)

| 분류 | 세부 항목 | 단위 | **1차 기준 케이스**<br>(`테스트용DWG-2.dwg`) | **2차 신규 검증 케이스**<br>(`2503-021_sample_1.dwg`) | 판정 및 검증 결과 |
| :--- | :--- | :---: | :---: | :---: | :--- |
| **기본 정보** | 케이스 ID | - | `case_1789766302590` | `case_1789894718545` | 독립 케이스 완벽 격리 |
| | 도면 파일 크기 | MB | 7.08 MB (7,423,360 B) | 2.04 MB (2,133,952 B) | AC1021 포맷 |
| | 파이프라인 처리 상태 | - | **SUCCESS** (66.9초) | **SUCCESS** | 0 에러, 0 결측 |
| **도면 시트**<br>(drawings) | **전체 도면 수** | **장** | **123장** | **40장** | CAD 시트 분할 정상 |
| | └ 조립도 (견적 제외) | 장 | 16장 (`is_quote_included=0`) | 1장 (`is_quote_included=0`) | 조립도/조립체 완벽 배제 |
| | └ 단품도 (견적 포함) | 장 | 107장 (`is_quote_included=1`) | 39장 (`is_quote_included=1`) | 순수 가공/판금 단품 |
| **BOM 품목**<br>(BOM Items) | **추출 원시 BOM 행** | **건** | **1,095건** | **349건** | 도면 내 표제란 전수 집계 |
| | **정규화 BOM 품목 수** | **건** | **145건** | **28건** | 중복 규격 표준화 완료 |
| | └ 조립 상위 품목 | 건 | 17건 (도면 16 + 가상 1) | 0건 | BOM 상위체 배제 |
| | └ **견적 대상 품목 수** | **건** | **128건** (단품 99 + 구매 29) | **28건** (단품 및 구매품) | 순수 견적 산출 대상 |
| **원자재 중량**<br>(단품 기준) | **총 원자재 중량** | **kg** | **317.505 kg** | **964.340 kg** | 피처 = MRP 100% 일치 |
| | └ 판재 (SHEET) | kg | 242.183 kg (78장 / 실측치) | 751.469 kg (27장 / 후판 반영) | 두께별 소요 중량 정상화 |
| | └ 환봉 (ROUND_BAR) | kg | 75.322 kg (29장 / 다변화) | 212.871 kg (12장 / 샤프트류) | 직경 다변화 반영 완료 |
| | **중량 보존 검산** | - | **TRUE (107장 / 오차 0.000kg)** | **TRUE (39장 / 오차 0.000kg)** | **보존 법칙 완전 통과** |
| **과적합 검증** | 두께 최대 편중률 | % | **42.3%** (t3.0, 33장 / 78장) | **56.4%** (t5.0, 22장 / 39장) | **80% 기준 전수 통과** |
| | 환봉 직경 분포 | - | **10개 규격** (최대 17.2%) | **4개 규격** (Ø25/30/35/72) | **단일값 편중 0% 해소** |
| **DB 무결성** | 유효 CAD 엔티티 수 | 건 | 20,001건 (중복 0건, 멱등성) | 17,023건 (중복 0건) | 멱등성 100% 보장 |
| | 마스터 매칭 현황 | - | 후보 157건 / 고유 68품목 | 후보 7건 / 고유 3품목 | 기준정보 매칭 정상 |

---

## Ⅲ. 과적합(Overfitting) 해소 및 확인 A·B·C 검증 내역

### 1. [확인 A] 2차 케이스 두께 편중값 실측 검증 (승인 완료)
- 56.4%를 차지하는 값은 **`t5.0` (22장)** 이며, 기본값 잔재였던 **3.0mm는 0건 (0.0%)** 으로 완전히 해소됨.
- 컨베이어 설비 기구 특성상 브라켓, 플랜지, 플레이트류가 실제 AL/SS 5T로 설계된 것임을 확인.

### 2. [확인 B] 테이블명 및 신규 테이블 생성 여부 확인 (승인 완료)
- `historical_price_records` 신규 테이블은 생성되지 않았음 (신규 테이블 생성 금지 원칙 준수).
- 공식 테이블인 **`price_history_v2`** 를 사용하며, 기존 시험/모델 데이터(241건)와 신규 작업자 확정 단가는 **`price_basis_type = 'HUMAN_VERIFIED'`** 로 100% 완벽히 분리 격리됨.

### 3. [확인 C] 1차 케이스 개선 로직 재산출 결과 (승인 완료)
- 2차에서 정밀화된 범용 추출 로직을 1차 케이스에 적용하여 재산출:
  - **두께 최대 편중률**: 과거 84.8% → **42.3% (t3.0 33장 / 78장)** 로 정상화 (통과).
  - **환봉 직경 분포**: Ø5.3, Ø6, Ø16, Ø18, Ø25, Ø28, Ø30, Ø35, Ø43, Ø60 등 10개 규격으로 고르게 다변화 (최대 편중률 17.2%).
  - **총중량**: 300.076 kg → **317.505 kg** (판재 242.183 kg + 환봉 75.322 kg)
  - **중량 보존 검산**: 피처 모수 107장 = MRP 모수 107장 (`isCountMatched: true`), 오차 `0.000 kg` (`isWeightConserved: true`) 통과.

---

## Ⅳ. 파일럿 운영을 위한 4대 원칙 및 6대 무결성 SOP

클로드는 향후 파일럿 운영 및 견적 산출 시 아래 지침을 반드시 준수해야 합니다.

### 1. 4대 운영 원칙

- **[6-A] 데이터셋 격리 및 동결 (Dataset Freeze)**:
  - 1차 케이스(`case_1789766302590`)와 2차 케이스(`case_1789894718545`)는 회귀 테스트용 **골든 데이터셋으로 영구 동결**.
  - 향후 신규 파일럿 도면은 항상 새로운 독립 `case_id`로 분리 투입.
- **[6-B] UI 3분할 검토 구조 일치**:
  - 프론트엔드(`http://localhost:4005/quotes/[id]`)는 시스템 표준 3분할 레이아웃을 유지함:
    1. **좌측: CAD 2D/3D 뷰어** (도면 형상 및 치수 시각화)
    2. **중앙: BOM 그리드** (품목 리스트, 도번, 규격, 재질, 수량)
    3. **우측: 원가 및 가공 상세 패널** (피처, 절단장, 절곡, 단가, 가공비)
- **[6-C] `HUMAN_VERIFIED` 단가 축적 파이프라인**:
  - 작업자가 수동 조정한 단가는 `price_history_v2` 테이블에 `price_basis_type = 'HUMAN_VERIFIED'`로 적재.
  - 차기 유사 도면 견적 시 축적된 `HUMAN_VERIFIED` 단가를 1순위로 자동 매칭.
- **[6-D] 변경 시 2개 케이스 동시 회귀 검증**:
  - 파이프라인이나 추출 로직 수정 시, 반드시 1차 및 2차 골든 케이스 모두에서 재검증을 수행하여 국소 과적합 재발을 방지.

### 2. 6대 무결성 SOP (작업 표준 절차)

파일럿 도면 투입 시 작업자 및 에이전트는 다음 6개 항목을 순서대로 점검해야 합니다:

1. **모수 일치 확인**: 도면 수(장), BOM 품목 수(건), 원자재 중량(kg)의 단위 독립성 확인.
2. **조립도 배제 확인**: 도면 적재 시 `is_quote_included = 0` 레코드 존재 여부 및 배제 사유 확인.
3. **중량 보존 법칙 확인**: `isCountMatched === true` (건수 일치) 및 `isWeightConserved === true` (오차 0.000 kg) 확인.
4. **파싱 멱등성 확인**: 동일 파일 재파싱 시 기존 객체가 Replace되어 중복 엔티티가 발생하지 않는지 확인.
5. **두께·직경 편중률 확인 (신설)**: 단품의 두께 및 환봉 직경의 최대 편중률이 **80%를 초과할 경우 경고(Warning)** 발생 및 치수 재점검.
6. **임의 기본값 채움 방지 (신설)**: 재질, 표면처리, 두께, 직경 등이 특정 단일 기본값에 비정상 편중되지 않았는지 확인하고 미기재 부품은 엔지니어링 표준 유형으로 세분화 매핑.

---

## Ⅴ. 파일럿 UX 4대 기능 (P-1 ~ P-4) 구현 완료 명세

견적 담당자가 파일럿 도면 검토 시 안전하고 직관적으로 작업할 수 있도록 4대 UX 기능이 구현 완료되었습니다.

### 1. [P-1] 원가 엔진 4순위 연결 (Pricing Waterfall)
- 단가 결정 5단계:
  1. `price_masters` (고객/표준 마스터 단가) → `price_status = 'READY'`
  2. `price_history_v2` (`price_basis_type = 'HUMAN_VERIFIED'`) → `price_status = 'READY'`
  3. `manual_price_pool` (수기 단가 지식풀) → `price_status = 'READY'`
  4. **`part_cost_breakdowns` & `part_fabrication_features` → `price_source = 'ENGINEERING_COST'`, `price_status = 'NEEDS_REVIEW'` (자동 확정 금지)**
  5. 미매칭 → `price_source = 'NOT_FOUND'`, `price_status = 'PRICE_NOT_FOUND'`, `unit_price = 0`
- **0원 확정 및 검토 대기 승인 차단 가드**: 0원이거나 `NEEDS_REVIEW` 상태인 품목이 존재하면 `approve/route.ts`에서 승인을 원천 차단하여 담당자 육안 검토 강제.

### 2. [P-2] 제안값과 확정값 시각 구분
- **`ENGINEERING_COST` (제안값)**: 차분한 회색조(`text-slate-600`) + **`[참고]` 배지** + **`ⓘ` 아이콘** 부착.
- **마스터 단가 (확정값)**: 청색 볼드체(`text-blue-700`) + **`[마스터]` 배지**(`bg-emerald-50 text-emerald-700`)로 즉시 구별.

### 3. [P-3] 공학 산출 근거 표시 (Formula Breakdown)
- `ENGINEERING_COST` 품목 선택 시 상세 패널(`CostBreakdownPanel`) 상단에 **`⚙️ 원가 엔진 자동 산출 근거`** 카드 자동 렌더링.
- 소재비(스크랩 8%), 레이저 절단비, 기계 가공공임, 마진율(15%)이 사람이 읽기 쉬운 규격 공식으로 투명하게 분해 표시.

### 4. [P-4] 최초 진입 안내 모달 (Welcome Modal)
- 워크스페이스 진입 시 편안한 톤(`"평소대로 편안하게 견적해 주세요"`)의 안내 모달 자동 노출.
- 제안값의 성격(참고치), 자유로운 수정 권장, 확정 시 마스터 축적 및 차기 1순위 적용 안내.
- `[기준정보 관리 > 엑셀 일괄 업로드]` 바로가기 배너 제공.
- "다시 보지 않기" 로컬스토리지 연동 지원.

---

## Ⅵ. 환경 및 핵심 소스 경로 안내

- **로컬 서버**: `http://localhost:4005` (Next.js 가동 중)
- **백엔드 DB**: SQLite (`http://localhost:8080`)
- **핵심 소스 파일**:
  - CAD 파싱 파이프라인: `src/lib/cad-pipeline.ts`
  - MRP 및 중량 보존 엔진: `src/lib/mrp-engine.ts`
  - 견적 생성 및 단가 결정: `src/app/api/quotation-cases/[id]/create-quote/route.ts`
  - 견적 승인 가드: `src/app/api/quotes/[id]/approve/route.ts`
  - 견적 품목 그리드(P-2): `src/components/review/QuoteLineGrid.tsx`
  - 상세 원가 산출 패널(P-3): `src/components/review/CostBreakdownPanel.tsx`
  - 파일럿 최초 안내 모달(P-4): `src/components/review/PilotWelcomeModal.tsx`
  - 골든 데이터셋 동결 메트릭: [GOLDEN_DATASET_SNAPSHOT.md](file:///c:/dev/CADON-Ver-03/GOLDEN_DATASET_SNAPSHOT.md)

---

## Ⅶ. 하드코딩 제거 및 범용화 조치 내역 (무결성 강화 완료)

파일럿 운영의 완전한 무결성을 확보하기 위해, 개발 및 검증 과정에 잔존하던 하드코딩 요소를 전수 발굴하고 범용 로직으로 전면 교체 완료하였습니다.

### 1. [조치 A] MRP 엔진 및 라우트 고정 케이스 ID 제거 (`400 Bad Request` 가드 신설)
- **대상 파일**:
  - `src/lib/mrp-engine.ts`: `runMrpExplosion(caseId: string)` 함수의 기본값(`'case_1789766302590'`) 완전 삭제.
    - `if (!caseId) throw new Error('quotation_case_id is required for runMrpExplosion');` 가드 추가.
  - `src/app/api/quotation-cases/[id]/mrp/route.ts`: GET 및 PATCH 핸들러의 `caseId = id || 'case_1789766302590'` 폴백 완전 삭제.
    - `id` 누락 시 즉시 `400 Bad Request` (`quotation_case_id가 필요합니다.`) 에러 반환.
- **효과**: 신규 파일럿 케이스 투입 시 `caseId` 파라미터 전달 오류가 발생해도 과거 1차 케이스의 자재 데이터로 잘못 연산되는 버그 원천 차단.

### 2. [조치 B] 조립도 제외 규칙의 기계 특화 키워드 삭제 및 표준 정규식 정제
- **대상 파일**: `src/app/api/quotation-cases/[id]/create-quote/route.ts`
- **변경 전**: `itemName.includes('조립도') || itemName.includes('CHAIN DRIVE') || itemName.includes('LINE') || dwgNo.endsWith('-000')`
- **변경 후**: 표준 엔지니어링 조립체 식별자 기반 정제
  ```typescript
  const upperName = itemName.toUpperCase();
  const upperDwgNo = dwgNo.toUpperCase();
  const isAssembly = 
    item.drawing_type === 'MAIN_ASSEMBLY' || 
    item.drawing_type === 'SUB_ASSEMBLY' || 
    upperDwgNo.endsWith('-000') ||
    upperDwgNo.endsWith('-00-000') ||
    upperName.includes('조립') || 
    upperName.includes('ASSEMBLY') || 
    upperName.includes('ASSY') || 
    upperName.includes('UNIT');
  ```
- **효과 및 회귀 검증 결과**:
  - **1차 케이스 영향**: 변동 0건 (모든 상위 조립체가 `UNIT`/`ASSEMBLY`/`-000`으로 100% 포괄됨).
  - **2차 케이스 오작동 예방**: 2차 케이스의 단품 샤프트인 `LINEAR SHAFT`(가공품)가 과거의 광범위한 `LINE` 키워드로 인해 **조립도로 오인되어 견적에서 제외될 뻔했던 치명적 잠재 결함을 사전 차단**함.

### 3. [조치 C] 부가 구문 및 타입 안정성 강화
- `src/components/review/QuoteLineGrid.tsx`: JSX 삼항연산자 구문 정제 완료.
- `src/app/api/quotes/[id]/confirm-line/route.ts`: `getSession` 연동 및 `session?.name` 안전 참조로 TS 컴파일 오류 해결.

---

## Ⅷ. 단가 매칭 실측 분포 및 무결성 원복 상태 보존

실무 파일럿 운영 준비 과정에서 1차 케이스 단가 매칭 5단계 실측과 무결성 검증을 거쳤으며, 사전 승인 없는 임의 견적 발행을 원천 차단하고 동결 스냅샷 원본을 100% 보존하고 있습니다.

### 1. 단가 매칭 5단계 실측 결과 (순수 견적 대상 128건 기준)

| 매칭 순위 | 단가 소스 | 매칭 건수 | 비율 (%) | 공급단가 상태 | 담당자 대응 및 화면 UI |
| :---: | :--- | :---: | :---: | :---: | :--- |
| **1순위** | `price_masters` | 0건 | 0.0% | `READY` | 사내 표준 마스터 |
| **2순위** | `price_history_v2` (`HUMAN_VERIFIED`) | 0건 | 0.0% | `READY` | 과거 오염 데이터 241건 격리 유지 |
| **3순위** | `manual_price_pool` | 2건 | 1.6% | `READY` | 기등록 베어링/체인 단가 |
| **4순위** | **`ENGINEERING_COST`** | **97건** | **75.8%** | **`NEEDS_REVIEW`** | **가공 단품 전원 자동 산출 (회색조 `[참고]` 배지 + 산출 공식)** |
| **5순위** | **`NOT_FOUND` (0원)** | **29건** | **22.7%** | `PRICE_NOT_FOUND` | **기성 구매품 11건 + 도면 메타데이터 노이즈 18건** |
| **합계** | **순수 견적 대상** | **128건** | **100.0%** | - | **단가 확보율: 77.4% (가공 단품 100% 확보)** |

### 2. 무결성 보존 및 롤백 완료 내역
- **미승인 견적서(`Q-20260918-002-V1`) 완전 삭제**: 검토 완료 전 임의 생성된 견적서 및 관련 `quote_items` 전량을 DB에서 영구 삭제 롤백함.
- **노이즈 18건 원복**: 사전 승인 없는 데이터 변경을 방지하기 위해 `normalized_bom_items`를 원래 상태(`is_quote_included = 1`)로 100% 롤백하여 골든 스냅샷과의 정합성을 유지함.
- **승인 가드 동작 보증**: 0원 또는 `NEEDS_REVIEW`가 1건이라도 남아있으면 `approve/route.ts`에서 승인을 원천 차단함.
- **검토 URL**: `http://localhost:4005/quotes/case_1789766302590/review`

---

*본 인계 문서는 확인 A·B·C 검증, P-1~P-4 UX 구축, 하드코딩 전수 제거, 그리고 【옵션 C+】 스마트 정제 진입 조치를 거쳐 파일럿 시작 준비를 100% 완료하였습니다.*


