# [CADON-Ver-03] 시스템 데이터 무결성 검증 완료 및 파일럿 운영 인계 보고서

> **문서 목적**: 본 문서는 `CADON-Ver-03` 프로젝트의 과거 데이터 불일치 이슈(조치 1~5) 해결 내역, 1차 기준 케이스 및 2차 신규 검증 케이스의 과적합 해소 실측 비교, 그리고 파일럿 운영 단계(6-A ~ 6-D) 진행을 위해 **클로드(Claude)에게 정식 인계하는 공식 종합 보고서**입니다.

- **작성 일시**: 2026-09-20 18:10 (KST)
- **개발/미리보기 포트**: `http://localhost:4005` (Next.js Dev Server)
- **EGDesk Backend/API**: `http://localhost:8080` (SQLite DB 연동)
- **대상 케이스**:
  - **1차 기준 검증 케이스**: `case_1789766302590` (`테스트용DWG-2.dwg`, 7.08 MB)
  - **2차 신규 검증 케이스**: `case_1789894718545` (`2503-021_sample_1.dwg`, 2.04 MB)

---

## Ⅰ. 핵심 무결성 조치 내역 (조치 1 ~ 조치 5)

과거 발생했던 모수 및 중량 수치 혼선(107 / 123 / 133 / 145 및 300.076kg vs 304.900kg)의 기술적 원인을 전수 규명하고 DB 및 소스 코드 레벨에서 조치를 완료하였습니다.

### 1. [조치 1] 조립도 16장 DB 배제 영구 기록 (`is_quote_included = 0`)
- **원인 규명**:
  - 과거 보고의 "조립도 12건 제외"는 DB 상태가 아닌 단순 프론트엔드 문자열 검색 필터에 불과했음. 실제 DB에는 `is_quote_included = 0` 레코드가 0건(전체 123장이 1로 방치)이었음.
  - 실제 조립도는 한글 명칭('조립도') 12건 외에 영문 명칭 조립체(`MAIN/SUB CHAIN DRIVE 01~04-000`, `SR1-000`) 4건이 더 존재하여 총 16건이었음.
- **조치 완료**:
  - `drawings` 테이블의 조립도 16건 전수에 대해 `is_quote_included = 0`, `exclude_reason = '조립도 (가공품 제외)'` 물리적 DB 업데이트 완료.
  - `src/lib/cad-pipeline.ts` 수정: 향후 어떤 DWG 도면이 파싱되더라도 도면 적재 시점(Ingestion time)에 조립도가 자동으로 `is_quote_included = 0` 처리되도록 파이프라인 영구 보강.
  - 단품 107장은 `is_quote_included = 1`로 명확히 분리.

### 2. [조치 2] 중량 집계 단일화 및 모수 일치 검산(`isCountMatched`) 신설
- **원인 규명**:
  - `part_fabrication_features` 123건 단순 합산(300.076 kg)과 `mrp-engine.ts`(304.900 kg)의 +4.824 kg 불일치 원인은 **"동일 도번(dwgNo)을 공유하는 복수 시트 도면"** 때문이었음.
  - 기존 MRP 엔진이 `Map<string, any>`의 Key를 도번(`dwgNo`)으로 매핑하면서 뒤쪽 시트 피처가 앞쪽 시트 피처를 덮어써 중량이 왜곡됨.
- **조치 완료**:
  - `mrp-engine.ts`를 고유 `drawing_id` 기반 1:1 매핑으로 전면 개편.
  - **검산 규칙 신설**: `isCountMatched`(모수 일치) 검증을 추가하여, **품목 건수(모수)가 다르면 합계 중량이 같더라도 중량 보존 실패(`isWeightConserved = false`)로 엄격 차단**.
  - 단품 107건 기준 피처 테이블 합산치 = MRP 엔진 산출치 = **300.076 kg (오차 0.000 kg)**으로 100% 일치 확정.

### 3. [조치 3 & 조치 5] 단위 표기 분리 및 과거 오보고(133건 / F7 1건) 공식 정정
- **과거 133건 유래 규명**: 전체 BOM 145건에서 과거 부분 조립도 12건을 단순 차감했던 수치(145 - 12 = 133)였음을 확인하고 완전히 폐기.
- **단위 분리 원칙 확립**:
  - 도면 장수: **장** (Sheets)
  - BOM 품목 수: **건** (Items)
  - 원자재 중량: **kg** (Mass)
  - 3개 단위를 혼용하여 "107건", "123품목" 등으로 표기하는 것을 금지.
- **마스터 매칭 정정**: 과거 '단가풀 F7 1건' 오보고를 폐기하고, 실제 후보 157건 / 고유 매칭 68품목으로 정정 반영.

### 4. [조치 4] 파싱 Idempotent(멱등성) 처리 및 중복 누적 데이터 물리 정리
- **원인 규명**: 동일 파일 3회 파싱으로 `cad_objects` 엔티티가 60,003건 중복 누적되어 있었음.
- **조치 완료**:
  - 구버전 2회분(40,002건) 물리 삭제 완료.
  - 구버전 `cad_parse_runs` 2건은 감사(Audit) 이력 보존을 위해 `status = 'SUPERSEDED'`로 안전 전이.
  - 최신 1회분(`parse_1789887403093`) 20,001건만 유효 유지.
  - `src/lib/cad-pipeline.ts`에 재파싱 시 기존 객체 자동 정리(Replace) 멱등성 로직을 영구 반영.

---

## Ⅱ. [1차 기준 케이스 vs 2차 신규 케이스] 최종 확정 실측 비교표

> **단위 분리 준수**: 도면 수(장), BOM 품목(건), 중량(kg)

| 분류 | 세부 항목 | 단위 | **1차 기준 케이스**<br>(`테스트용DWG-2.dwg`) | **2차 신규 검증 케이스**<br>(`2503-021_sample_1.dwg`) | 판정 및 검증 결과 |
| :--- | :--- | :---: | :---: | :---: | :--- |
| **기본 정보** | 케이스 ID | - | `case_1789766302590` | `case_1789894718545` | 독립 케이스 완벽 격리 |
| | 도면 파일 크기 | MB | 7.08 MB (7,423,360 B) | 2.04 MB (2,133,952 B) | AC1021 포맷 |
| | 파이프라인 처리 상태 | - | **SUCCESS** | **SUCCESS** | 0 에러, 0 결측 |
| **도면 시트**<br>(drawings) | **전체 도면 수** | **장** | **123장** | **40장** | CAD 시트 분할 정상 |
| | └ 조립도 (견적 제외) | 장 | 16장 (`is_quote_included=0`) | 1장 (`is_quote_included=0`) | 조립도/조립체 완벽 배제 |
| | └ 단품도 (견적 포함) | 장 | 107장 (`is_quote_included=1`) | 39장 (`is_quote_included=1`) | 순수 가공/판금 단품 |
| **BOM 품목**<br>(BOM Items) | **추출 원시 BOM 행** | **건** | **1,095건** | **349건** | 도면 내 표제란 전수 집계 |
| | **정규화 BOM 품목 수** | **건** | **145건** | **28건** | 중복 규격 표준화 완료 |
| | └ 조립 상위 품목 | 건 | 17건 (도면 16 + 가상 1) | 0건 (단품 및 외주품 위주) | BOM 상위체 배제 |
| | └ **견적 대상 품목 수** | **건** | **128건** (단품 99 + 구매 29) | **28건** (단품 및 구매품) | 순수 견적 산출 대상 |
| **원자재 중량**<br>(단품 기준) | **총 원자재 중량** | **kg** | **300.076 kg** | **964.340 kg** | 피처 = MRP 100% 일치 |
| | └ 판재 (SHEET) | kg | 138.567 kg (64장 / 17규격) | 751.469 kg (27장 / 후판 중량 반영) | 두께별 소요 중량 정상화 |
| | └ 환봉 (ROUND_BAR) | kg | 161.509 kg (43장 / 19규격) | 212.871 kg (12장 / 샤프트류) | 직경 다변화 반영 완료 |
| | **중량 보존 검산** | - | **TRUE (107장 / 오차 0.000kg)** | **TRUE (39장 / 오차 0.000kg)** | **보존 법칙 완전 통과** |
| **데이터베이스**<br>(DB 무결성) | 유효 CAD 엔티티 수 | 건 | 20,001건 (중복 40,002건 삭제) | 17,023건 (중복 0건) | 멱등성 100% 보장 |
| | 마스터 매칭 현황 | - | 후보 157건 / 고유 68품목 | 후보 7건 / 고유 3품목 | 기준정보 매칭 정상 |

---

## Ⅲ. 2차 검증 케이스 과적합(Overfitting) 해소 내역

2차 도면 투입 직후 발견되었던 과적합 징후 3가지를 정밀 실측하여 완전히 해소하였습니다.

### 1. 두께 편중(3.0mm 82.5%) 해소
- **원인**: 초기 간이 파싱 시 기본값 3.0mm에 편중되는 규칙이 남아있었음.
- **조치**: CAD 내 실제 치수선(DIMENSION) 및 텍스트 주기(t2.0, t4.5, t5.0, t6.0, t10.0, t25.0 등) 연동 규칙 정밀화.
- **검증 결과**: 최대 편중률 **56.4%로 정상화 (통과)**. 구조용 후판(t10, t25)이 정상 분리되어 실제 중량(판재 751.469 kg) 산출.

### 2. 환봉 직경 단일값(100%) 해소
- **원인**: 환봉 인식 시 특정 기본 직경으로 일괄 매핑되던 로직 잔재.
- **조치**: CAD 샤프트 외경 주석(Ø25, Ø30, Ø35, Ø72 등) 파싱 강화.
- **검증 결과**: Ø25, Ø30, Ø35, Ø72 등 다변화 정상 확인 (환봉 212.871 kg, 모수 12장 정상 집계).

### 3. 누락 조립체 배제 및 표제란 컬럼 밀림 정제
- 누락되었던 조립체(`C/V 연결 UNIT`) 1건을 `SUB_ASSEMBLY`로 분류하고 `is_quote_included = 0`, `exclude_reason = '조립도 (가공품 제외)'`로 영구 배제.
- 따라서 2차 도면은 **전체 40장 = 견적 제외 1장(조립) + 견적 대상 39장(단품)**으로 확정.
- 표제란 BOM에서 모터 사양/제조사 텍스트가 재질 컬럼으로 밀려 유입되던 현상 정제 완료.

---

## Ⅳ. 파일럿 운영 준비 및 실행 지침 (6-A ~ 6-D)

클로드는 본 시스템을 기반으로 파일럿 운영 시 다음 4가지 핵심 원칙을 엄수하여 작업을 수행해야 합니다.

### [6-A] 데이터셋 격리 원칙
- 1차 기준 케이스(`case_1789766302590`)와 2차 검증 케이스(`case_1789894718545`)는 회귀 테스트(Regression Test)용 **골든 데이터셋(Golden Dataset)**으로 동결(Freeze) 유지.
- 향후 신규 파일럿 도면 투입 시 반드시 독립된 새로운 `case_id`로 분리하여 적재.

### [6-B] 3분할 검토 워크스페이스
- 프론트엔드 UI(`http://localhost:4005/quotes/[id]`)에서 작업자는 다음 3단계 워크플로우를 거침:
  1. **조립도/단품도 분할 검토**: `is_quote_included` 플래그 및 배제 사유 육안 확인.
  2. **피처/중량 보존 검증**: 단품 시트 수와 피처 건수, MRP 중량의 오차(0.000 kg) 확인.
  3. **견적 대상 BOM 확정**: 표준 단품 및 외주 가공비 산출.

### [6-C] `HUMAN_VERIFIED` 단가 축적 파이프라인
- 견적 담당자가 UI에서 수동 보정한 단가 및 공정 정보는 `historical_price_records` 테이블에 `HUMAN_VERIFIED` 태그와 함께 축적.
- 축적된 검증 단가는 다음 견적 시 우선순위 1순위로 자동 매칭되도록 파이프라인 연계.

### [6-D] 4대 무결성 SOP (작업 표준 절차)
1. **모수 일치 확인**: 도면 수(장), BOM 품목(건), 중량(kg)의 분리 확인.
2. **조립도 배제 확인**: 도면 적재 시 `is_quote_included = 0` 레코드 존재 여부 체크.
3. **중량 보존 법칙 확인**: `isWeightConserved === true` 및 `isCountMatched === true` 필수 확인.
4. **멱등성 확인**: 재파싱 시 `cad_objects` 중복 적재 없이 Replace 처리 여부 확인.

---

## Ⅴ. 클로드(Claude) 개발 및 검증 환경 안내

- **로컬 서버**: `http://localhost:4005` (Next.js 가동 중, `npm run dev -- -p 4005`)
- **API 및 DB 엔드포인트**: `http://localhost:8080` (EGDesk MCP / Local API)
- **주요 소스 코드 경로**:
  - CAD 적재 및 파싱 파이프라인: `src/lib/cad-pipeline.ts`
  - 중량 산출 및 MRP 엔진: `src/lib/mrp-engine.ts`
  - 도면 상세 및 견적 UI: `src/app/quotes/[id]/page.tsx`
  - 원자재 피처 분석기: `src/lib/feature-extractor.ts`
- **검증용 DB 상태 확인 스크립트**:
  - `src/lib/mrp-engine.ts`의 `verifyWeightConservation(drawingIds)` 함수를 호출하여 임의의 케이스에 대해 즉시 중량 보존 및 모수 일치 여부 검증 가능.

---
*본 인계 보고서의 모든 수치와 로직은 SQLite DB 실측 및 Next.js dev 서버(포트 4005) 구동 테스트를 거쳐 최종 확정되었습니다.*
