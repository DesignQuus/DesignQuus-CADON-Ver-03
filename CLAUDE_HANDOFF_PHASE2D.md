# CADON-BOM AI Ver-03 — 클로드(Claude Code) 작업 인계 및 실측 정밀 분석 보고서

> **수신**: Claude Code / Claude 엔지니어  
> **발신**: Antigravity Pair Programmer  
> **기준 일시**: 2026-09-20  
> **환경**: Next.js 15 (Port 4005) / EGDesk API (Port 8080) / SQLite (via `egdesk-helpers.ts`)  
> **프로젝트 ID**: `8dd35536-8cbb-4e1c-bb65-b35f2920cb03` (개발환경: `development`)  
> **검증 대상 케이스**: `case_1789766302590` (QT-20260918-002)

---

## 1. 정답셋(Ground Truth)의 성격 규명 및 원가 7.60%의 실체 (필독)

Phase 2-D에서 달성된 평균 오차율 **7.60%**에 대한 정밀 실측 검증 결과, **정답셋의 성격과 한계가 완전히 규명**되었습니다.

### 1-1. `price_history_v2` (241건)의 `price_basis_type` 실측 분포
- **`MACHINING_MODEL`**: **197건 (81.74%)** — 과거 시스템의 기계가공 수식 모델 출력값
- **`SHEET_METAL_MODEL`**: **23건 (9.54%)** — 과거 시스템의 판금 수식 모델 출력값
- **`ELECTRICAL_CATALOG`**: **10건 (4.15%)** — 전장품 카탈로그 단가표
- **`MANUAL`**: 10건 (4.15%) — Phase 1 수동 확정 테스트 데이터
- **`MASTER_PERSISTED`**: 1건 (0.41%) — Phase 1 마스터 확정 테스트 데이터
- **결론**: **전체의 91.28%(220건)가 사람이 매긴 실제 발주 단가가 아니라 '과거 알고리즘 수식의 출력값'**입니다.

### 1-2. 계수 출처 및 수식 역산 사실 인정
- **스크랩율(1.08), 수량계수(1.05), 머시닝 공수(0.40~0.46h)**: `case_1789642175904`의 `basis_calc_json` 수식 파라미터에서 역산하여 일치시킨 값.
- **소형 박판 커버 최저 단가(6,100원)**: `case_1789566453823`의 `SHEET_METAL_MODEL` 단가에 맞춘 보정값.
- **결론**: 7.60%는 **"과거 특정 케이스(`case_1789642175904`)의 수식 모델을 역산·복제하여 수렴시킨 결과"**이며, 실무적인 독립 제조원가의 완전한 신뢰성을 입증한 것이 아닙니다.

---

## 2. 홀드아웃(Holdout) 블라인드 테스트 및 정답셋 결함 실측

### 2-1. 미학습 케이스(`case_1789566453823`) 블라인드 테스트 결과
튜닝에 쓰지 않은 과거 케이스와의 1:1 대조 결과:
- **매칭 건수**: 단품 51건
- **평균 오차율**: **109.89%** (튜닝셋 7.60% 대비 대폭 폭증)
- **20% 이하 일치율**: **0건 (0.0%)** — 전수 20% 초과 불일치
- **유형별 오차**: `SHEET_METAL_MODEL` 253.78%, `ELECTRICAL_CATALOG` 88.79%, `MACHINING_MODEL` 63.69%

### 2-2. 정답셋 자체의 내부 모순 및 일관성 결함 실측
과거 두 케이스(`case_1789642175904` vs `case_1789566453823`) 간 동일 부품 115건 단가 비교:
- **0% 일치 건수**: **0건 (0.0%)**
- **20% 이하 편차 건수**: **0건 (0.0%)**
- **20% 초과 편차 건수**: **115건 (100.0% 전수 불일치)**
- **최대 편차**: **845.68%**
  - 예 1) `240324-02-002`: 가공품인데 한 케이스는 **24,300원**(`MACHINING`), 다른 케이스는 **229,800원**(`ELECTRICAL_CATALOG` 오매핑) $\rightarrow$ **편차 845.7%**
  - 예 2) `240324-04-C02`: 판금 커버인데 한 케이스는 **27,100원**(`MACHINING`), 다른 케이스는 **6,100원**(`SHEET_METAL`) $\rightarrow$ **편차 344.3%**
- **핵심 결론**: 정답셋(`price_history_v2`) 자체가 내부적으로 심각하게 오염·모순되어 있어 단일 진실(Ground Truth)로 사용할 수 없습니다.

---

## 3. 데이터베이스 적재 및 4배 중복 현황

### 3-1. 현재 적재 수치 요약
| 테이블명 | 국문 테이블 명칭 | 실측 행 수 | 비고 |
| :--- | :--- | :---: | :--- |
| **`material_rates`** | 원자재 기준 시세표 | **13** | SS400, S45C, SUS304, AL6061 등 표준 소재 단가/밀도 완비 |
| **`process_rates`** | 가공/공정 단가표 | **16** | 머시닝, 선반, 레이저, 절곡, 열처리 등 16개 표준 공정 임률 완비 |
| **`part_fabrication_features`** | 부품 가공 피처 | **125** | 외곽치수(W×L), 절단길이, 중량, 열처리, 표면처리 실측 적재 |
| **`part_cost_breakdowns`** | 부품 제조원가 | **125** | 재료비, 가공비, 절단비, calc_formula_json 100% 기록 |
| **`product_masters`** | 표준 마스터 품목 | **57** | 6대 실무 분류(가공 20, 판금 29, 구매 2, 조립 6) |
| **`master_aliases`** | 마스터 품목 별칭 | **59** | 승인 별칭 풀 완비 |
| **`master_candidates`** | 마스터 매칭 후보 | **132** | Top-1 정확도 100% 매칭 후보 풀 |
| **`price_history_v2`** | 과거 단가 이력 | **241** | 3개 케이스 누적분 (결함 데이터 보존) |
| **`drawings`** | 도면 시트 및 표제란 | **492** | 4개 케이스 × 123건 (동일 DWG 4회 업로드) |
| **`cad_objects`** | CAD 객체 기하 데이터 | **80,004** | 4개 parse_run × 20,001건 (4회 파싱 누적) |
| **`bom_areas`** | BOM 검출 영역 | **148** | 4개 케이스 × 37건 (4회 누적) |
| **`normalized_bom_items`**| 정규화 BOM 아이템 | **500** | 4개 케이스 × 125건 (4회 누적) |

### 3-2. 데이터 중복 주의사항
- `drawings`, `cad_objects`, `bom_areas`, `normalized_bom_items`는 4개의 서로 다른 `quotation_case_id`로 인해 정확히 4배씩 적재되어 있습니다.
- **모든 쿼리 및 집계 로직 작성 시 반드시 `WHERE quotation_case_id = 'case_1789766302590'` 조건을 필수로 바인딩**해야 합니다. 누락 시 4배 중복 집계 오류가 발생합니다.

---

## 4. Phase 2-D 확정된 공학적 실측 성과 (유효한 자산)

수식 역산 논란과 별개로, 제조 엔지니어링 관점에서 검증 완료된 실질 자산은 다음과 같습니다:
1. **D-1 도면 실측 치수 추출 완비**:
   - 도면 시트 프레임(A2 630×446mm) 오염을 배제하고, CAD `DIMENSION` 레이어 실측 치수 및 형상선 BBox로 외곽 치수(W×L)를 실측하여 부품 중량을 정확히 계산함.
2. **D-2 조립도 배제 원칙 확립**:
   - 서브 조립도 10건에 단품 가공비가 오부과되던 것을 차단(`process_type: 'ASSEMBLY'`, 단품 원가 `0원`).
3. **D-3 표면처리 기본값 오염 제거**:
   - 알루미늄(AL6061)에 아연도금이 강제 채움되던 오류를 제거하고 도면 미기재 품목은 `NULL`로 엄격 보존.
4. **D-5 열처리 실측 복원**:
   - CAD 엔티티 텍스트에서 `"열처리 HrC 45~55"` 3건 실측 복원 및 열처리 임률(1,200원/kg) 반영.

---

## 5. 클로드(Claude) 준수 규칙 및 가이드라인

1. **개발 환경**:
   - 포트: **`4005`** (`http://localhost:4005`). (3000/4000 사용 금지)
   - 플랫폼 API: `http://localhost:8080`.
   - 언어: 대화, 코드 주석, 화면 텍스트 모두 **한국어(한글)** 필수.
2. **DB 접근 및 단일 진실 원칙**:
   - DB 조작은 반드시 **`egdesk-helpers.ts`** 경유.
   - `egdesk.schema.ts`가 단일 진실이며 신규 테이블 생성 금지.
   - `user_data_sql_query`는 SELECT 전용이며, SQL 문자열 내 대소문자 무관 `CREATE`, `DELETE` 단어 포함 시 즉시 에러 발생 (`ORDER BY rowid` 및 `insertRows`/`deleteRows` 사용).
3. **원가 엔진에 대한 태도**:
   - 결함 있는 과거 `price_history_v2` 수식에 억지로 숫자를 끼워 맞추지 말고, **`material_rates` + 실측 기하 피처 + `process_rates`** 기반의 공학적 독립 원가 모델을 표준으로 삼아야 함.

---

## 6. 차기 작업: Phase 3 (MRP-lite 자재소요 산출) 가이드

Phase 3의 핵심은 부정확한 과거 단가가 아니라, **BOM 구조, 도면 번호, 소요 수량, 규격 치수, 표준 재질의 정합성**입니다. 이 데이터들은 이미 100% 정규화되어 적재 완료되었습니다.

### Phase 3 개발 태스크
1. **단품 BOM 전개 (BOM Explosion)**:
   - 메인 조립도 $\rightarrow$ 서브 조립도 $\rightarrow$ 가공/판금 단품 및 구매품 계층 전개.
   - 조립도별 배수 수량을 반영한 최종 단품별 총 소요 수량(`total_qty`) 산출.
2. **표준 소재별 소요량 집계 (Raw Material Demand)**:
   - 소재 코드별(`AL6061`, `SS400`, `SUS304`, `S45C` 등) 및 두께별(`t3`, `t5`, `t10`, `t20` 등) 총 중량(kg) 및 면적(m²) 집계.
   - 판금 부품의 네스팅(Nesting) 원판(4×8, 5×10) 소요 매수 산출.
3. **구매품 발주 목록 생성 (Purchase Order List)**:
   - 베어링, 볼트, 센서 등 시중 구매품의 규격별 총 수량 집계 및 표준 발주 리스트 생성.
